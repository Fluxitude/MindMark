# MindMark Environment Variables
# Copy this file to .env.local and fill in your actual values
# NEVER commit .env.local to git - it contains sensitive information

# Supabase Configuration (REQUIRED)
NEXT_PUBLIC_SUPABASE_URL=your_supabase_project_url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key
SUPABASE_SERVICE_KEY=your_supabase_service_role_key

# Security (REQUIRED)
JWT_SECRET=your_jwt_secret_min_32_chars

# Application URLs
NEXT_PUBLIC_APP_URL=http://localhost:3000
NEXT_PUBLIC_WEB_URL=http://localhost:3001
NODE_ENV=development

# AI Services (Optional)
OPENAI_API_KEY=your_openai_api_key
ANTHROPIC_API_KEY=your_anthropic_api_key

# Content Extraction (Optional)
FIRECRAWL_API_KEY=your_firecrawl_api_key

# Typesense Search (REQUIRED for search functionality)
TYPESENSE_HOST=xxx.a1.typesense.net
TYPESENSE_PORT=443
TYPESENSE_PROTOCOL=https
TYPESENSE_API_KEY=your_typesense_api_key

# Browser Automation (Optional)
BROWSERBASE_API_KEY=your_browserbase_api_key
BROWSERBASE_PROJECT_ID=your_browserbase_project_id

# Workflow Orchestration (Optional)
MASTRA_API_KEY=your_mastra_api_key

# Analytics and Monitoring (Optional)
VERCEL_ANALYTICS_ID=your_vercel_analytics_id
SENTRY_DSN=your_sentry_dsn

# Instructions:
# 1. Copy this file: cp .env.example apps/app/.env.local
# 2. Fill in your actual values (get new keys from Supabase dashboard)
# 3. Generate JWT secret: openssl rand -base64 32
# 4. NEVER commit .env.local to git
